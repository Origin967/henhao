项目开发通用执行规范

一、总则

本规范适用于本项目所有代码开发、代码修改、功能开发、功能添加、功能删除、BUG修复、UI调整、动画优化、性能优化、安全优化、代码重构、功能升级、架构调整以及其他任何形式的项目修改。

所有AI、开发人员及其他参与项目开发的人员，在执行任何代码修改或新增任务之前，必须先完整阅读并理解本规范。

在开始修改代码之前，必须先完整分析当前项目，包括但不限于：

1. 项目目录结构；
2. 文件之间的关系；
3. 模块之间的依赖关系；
4. 当前功能逻辑；
5. 数据结构；
6. 数据流；
7. 调用流程；
8. 事件处理流程；
9. 公共组件；
10. 公共函数；
11. 样式继承与覆盖关系；
12. 国际化架构；
13. 网络请求及数据来源；
14. 第三方依赖；
15. 当前已经存在的安全机制；
16. 当前已经存在的兼容性处理；
17. 当前功能与未来API、后端、数据库及管理后台的潜在关系。

禁止在不了解当前项目结构、代码关系和功能逻辑的情况下直接修改代码。

禁止盲目重构。

禁止为了完成当前需求而破坏已有功能。

禁止仅根据文件名称、代码片段或表面现象进行猜测式修改。

必须先理解现有实现，再确定最合理的修改方案。

二、最小改动原则

所有修改必须遵循最小改动原则。

只修改完成当前任务所必须修改的代码。

未经明确要求，禁止修改与当前任务无关的：

1. 功能；
2. UI；
3. 页面布局；
4. 颜色；
5. 字体；
6. 动画；
7. 交互；
8. 游戏逻辑；
9. 数据结构；
10. 音效；
11. 国际化内容；
12. 已经稳定运行的代码；
13. 其他现有模块。

禁止为了“顺便优化”而擅自修改其他内容。

如果发现当前项目存在与本次任务无关的问题，不允许直接修改。

必须先列出发现的问题、原因、影响及建议方案，由项目负责人确认后才能修改。

三、代码质量

所有代码必须保持高质量、清晰、稳定、可维护、可扩展。

禁止产生：

1. 重复代码；
2. 无意义的临时代码；
3. 无用代码；
4. 无效变量；
5. 无效函数；
6. 混乱的代码结构；
7. 难以维护的代码；
8. 隐式依赖；
9. 不必要的全局变量；
10. 为解决一个问题而产生大量新的技术债务。

代码应尽可能保持：

1. 模块化；
2. 高内聚；
3. 低耦合；
4. 职责清晰；
5. 命名清晰；
6. 结构清晰；
7. 易阅读；
8. 易调试；
9. 易维护；
10. 易扩展。

所有重复逻辑必须进行合理统一管理。

对于可以独立拆分的功能，应采用合理的模块化设计。

不得为了追求所谓“高级架构”而进行没有实际必要的大规模重构。

四、当前技术阶段与未来技术扩展

当前项目处于前端研发阶段，当前主要使用HTML、CSS、JavaScript开发游戏前端。

当前阶段不代表项目未来永久限制为HTML、CSS、JavaScript。

未来可以根据项目实际需求使用或引入任何合适的：

1. 前端技术；
2. 编程语言；
3. 前端框架；
4. 后端语言；
5. 后端框架；
6. API；
7. 数据库；
8. WebSocket；
9. 服务器；
10. 云服务；
11. 管理后台；
12. 其他必要开发技术。

不限制未来具体技术栈。

但是，任何新增技术、框架、SDK、依赖、服务器、API、数据库或第三方服务，都必须经过合理的架构、安全、兼容性和维护成本评估。

不能因为当前项目只有前端，就设计出完全无法连接未来后端的结构。

五、未来API、后端、数据库及管理后台兼容规范

虽然当前项目暂时没有后端、数据库、API和管理后台，但所有新增及修改功能都必须考虑未来接入这些系统的可能性。

代码设计必须尽量避免未来需要大规模重写。

对于未来可能由服务器、数据库、API或管理后台进行查询、修改、配置、同步或管理的数据，应提前考虑合理的数据结构、模块边界和数据来源设计。

包括但不限于：

1. 用户账户数据；
2. 登录数据；
3. 注册数据；
4. 用户状态；
5. 游戏配置；
6. 游戏参数；
7. 活动配置；
8. 活动时间；
9. 活动开关；
10. 公告；
11. Banner；
12. 活动文案；
13. 多语言资源；
14. 游戏规则；
15. 用户协议；
16. 隐私政策；
17. 系统通知；
18. 其他运营配置。

这些数据如果未来明显需要由后台管理，不得为了当前开发方便而无必要地完全硬编码在前端业务代码中。

应合理进行数据与核心业务逻辑分离。

代码应尽量做到：

前端负责展示、交互及客户端功能逻辑。

API负责数据通信。

后端负责服务端业务处理。

数据库负责数据持久化。

管理后台负责运营人员对允许管理的数据进行管理。

六、API预留原则

当前项目如果尚无真实后端，不得擅自创建不存在的服务器、数据库或虚假API。

不得为了所谓“未来兼容”而加入没有实际服务器支持的假接口、假请求或无意义网络请求。

但是，在设计数据结构和模块时，应合理预留未来API接入位置。

未来当真实后端建立后，应尽可能通过替换数据来源或增加数据适配层的方式接入，而不是大规模重写整个前端。

前端不得因为当前使用本地数据，就把数据结构设计成未来完全无法转换为API数据结构的形式。

如果当前功能明显属于未来需要服务器处理的功能，AI必须在修改前考虑未来服务端职责，并避免将关键业务逻辑完全依赖客户端。

七、数据与配置管理

所有可变化内容应尽可能与核心业务逻辑分离。

可变化内容包括但不限于：

1. 文案；
2. 配置；
3. 参数；
4. 游戏规则；
5. 活动状态；
6. 活动时间；
7. 页面内容；
8. 资源；
9. 多语言；
10. 其他运营数据。

不得无必要地将所有可变化内容直接写死在核心业务逻辑中。

未来需要后台管理的数据，应设计为可以通过配置、数据文件或API数据源进行替换。

代码负责“如何运行”。

配置和后台数据负责“运行什么内容”。

八、协议、隐私政策及动态文案管理

所有用户协议、隐私政策、游戏规则、活动规则、公告、帮助说明、FAQ、活动文案等用户可见内容，都必须与实际业务保持一致。

未来允许通过管理后台和API管理的动态内容，应提前考虑对应的数据结构。

对于普通运营文案，例如：

1. 公告；
2. 活动标题；
3. 活动内容；
4. Banner；
5. 活动时间；
6. 系统通知；

可以根据未来架构设计为后台动态配置。

对于用户协议、隐私政策等重要协议类内容，不得简单覆盖历史版本。

应支持合理的：

1. 版本号；
2. 发布时间；
3. 生效时间；
4. 历史版本；
5. 当前生效版本。

如果未来业务需要记录用户接受协议的情况，应支持记录用户接受的协议版本及接受时间。

九、国际化i18n

所有用户可见文字必须使用统一国际化i18n架构管理。

禁止将大量语言文字分散硬编码在业务代码中。

所有语言资源必须统一管理。

未来增加更多国家或地区语言时，应尽可能只增加对应语言资源，而不需要大量修改业务代码。

所有以下用户可见内容必须支持国际化：

1. 页面文字；
2. 按钮；
3. 提示；
4. 弹窗；
5. 状态；
6. 活动文案；
7. 用户协议；
8. 隐私政策；
9. 游戏规则；
10. 错误提示；
11. 网络异常提示；
12. 登录注册提示；
13. 其他用户可见内容。

语言切换必须立即生效。

不得要求用户刷新页面才能完成语言切换。

不得出现：

1. 旧语言缓存；
2. 部分内容未切换；
3. 中英文混合；
4. 其他语言混合；
5. 语言资源丢失；
6. 切换后显示错误。

所有新增功能必须遵循现有i18n架构。

禁止新增硬编码用户可见文字。

十、跨平台与设备兼容性

所有开发必须考虑跨平台兼容性。

目标运行环境包括但不限于：

1. Windows；
2. macOS；
3. Linux；
4. Android；
5. iOS；
6. HarmonyOS。

需要考虑不同品牌和设备，包括但不限于：

1. Apple；
2. Huawei；
3. Xiaomi；
4. OPPO；
5. vivo；
6. Samsung；
7. 其他主流设备。

必须考虑新系统版本与旧系统版本之间的兼容性。

所有设备应尽可能保持一致的视觉和交互体验。

需要考虑：

1. 屏幕尺寸；
2. 分辨率；
3. DPR；
4. 横竖屏；
5. 刘海屏；
6. 安全区域；
7. 字体差异；
8. 浏览器差异；
9. 触摸操作；
10. 鼠标操作；
11. 键盘操作；
12. 设备性能差异。

不得因为某一个设备优化而破坏其他设备。

十一、响应式设计

必须采用合理的响应式设计。

根据实际情况合理使用：

1. CSS clamp；
2. vw；
3. vh；
4. rem；
5. %；
6. aspect-ratio；
7. flex；
8. grid；
9. media query；
10. ResizeObserver；
11. 其他合理响应式技术。

不得通过大量针对单一设备型号的固定像素值进行粗暴适配。

必须尽可能保证不同屏幕尺寸下：

1. UI布局正常；
2. 按钮大小合理；
3. 文字不会溢出；
4. 元素不会重叠；
5. 游戏区域比例正确；
6. 触摸区域合理；
7. 动画正常；
8. 弹窗正常；
9. 横竖屏正常。

十二、UI、视觉及用户体验

所有UI设计、美观优化和交互设计必须符合2026年及未来现代商业级产品的审美标准。

整体视觉应保持：

1. 高级感；
2. 精致感；
3. 科技感；
4. 现代感；
5. 年轻化；
6. 商业级品质；
7. 视觉层次丰富；
8. 统一且完整的设计语言；
9. 良好的视觉反馈；
10. 高质量的细节表现。

避免廉价、过时、模板化和粗糙设计。

禁止出现明显的：

1. 廉价感；
2. 土气设计；
3. 过时设计；
4. 粗糙设计；
5. 模板化设计；
6. 低质量素材；
7. 随意拼接的UI；
8. 不协调的颜色；
9. 过度使用渐变；
10. 过度使用发光；
11. 没有层次的纯色块；
12. 生硬的动画；
13. 低质量图标；
14. 不统一的字体；
15. 不统一的圆角；
16. 不统一的阴影；
17. 不合理的间距；
18. 视觉元素堆砌；
19. 为了“看起来炫”而加入大量无意义特效。

科技感不等于简单堆叠蓝色、霓虹灯、发光边框和粒子效果。

如果使用科技感视觉元素，必须考虑整体设计语言、光影关系、层次关系、比例、动效节奏和用户体验。

所有视觉优化必须做到：

高级但不过度、科技但不俗套、精致但不复杂、炫酷但不廉价。

十三、视觉一致性规范

同一个项目中的所有页面、按钮、弹窗、图标、卡片、输入框、标题、文字、阴影、圆角、动画和交互反馈，应保持统一的视觉设计语言。

禁止不同页面出现明显不同的：

1. UI风格；
2. 圆角体系；
3. 字体体系；
4. 阴影体系；
5. 按钮体系；
6. 动画体系；
7. 颜色体系；
8. 间距体系。

如果项目已经存在成熟的视觉设计语言，新增功能必须优先继承现有设计系统，而不是重新创建完全不同的风格。

十四、交互设计

所有交互功能必须保证：

1. 点击准确；
2. 响应快速；
3. 操作流畅；
4. 状态明确；
5. 防止重复点击；
6. 防止误触；
7. 防止重复提交；
8. 防止状态不同步；
9. 提供合理反馈。

所有按钮、表单、弹窗和交互操作都必须考虑：

正常状态、加载状态、成功状态、失败状态、异常状态和重复操作状态。

十五、动画与特效

所有动画和特效必须自然、流畅、稳定。

禁止出现：

1. 卡顿；
2. 掉帧；
3. 动画冲突；
4. 闪烁；
5. 元素错位；
6. 动画状态无法恢复；
7. 重复创建大量动画实例；
8. 无意义资源浪费。

动画必须考虑不同设备性能。

高性能设备和低性能设备都应尽可能保持稳定运行。

十六、性能

所有性能优化必须兼顾高性能设备和低性能设备。

必须考虑：

1. CPU占用；
2. GPU占用；
3. 内存；
4. DOM数量；
5. Canvas性能；
6. 图片资源；
7. 音频资源；
8. 网络请求；
9. 动画数量；
10. 事件监听；
11. 定时器；
12. 内存泄漏。

禁止出现明显的：

1. 内存持续增长；
2. CPU异常占用；
3. 加载缓慢；
4. 无意义重复请求；
5. 大量重复计算；
6. 闪退；
7. 严重发热；
8. 页面卡死。

十七、安全开发规范

所有安全设计必须考虑未来服务器部署后的实际安全需求。

不能将客户端视为可信环境。

任何运行在用户设备上的HTML、CSS、JavaScript都可能被用户查看、修改或调试。

因此：

1. 关键数据不能仅依赖前端保护；
2. 关键权限不能仅依赖前端判断；
3. 关键游戏结果不能仅由客户端决定；
4. 重要奖励不能仅由客户端决定；
5. 关键账户状态不能仅由客户端决定；
6. 重要配置不能仅依赖客户端；
7. 未来服务器部署后必须进行服务端校验。

必须考虑防止：

1. 客户端数据篡改；
2. 参数篡改；
3. 伪造请求；
4. 异常请求；
5. 重放请求；
6. 简单自动化操作；
7. 恶意脚本；
8. 非法修改关键参数；
9. 未授权访问；
10. 数据泄露。

禁止将真正重要的安全逻辑完全放在前端。

十八、恶意代码、未授权行为与第三方代码安全规范

任何AI、开发人员或第三方代码均不得在未经明确要求和授权的情况下，在项目中加入任何恶意代码、隐藏功能或未授权行为。

禁止加入：

1. 后门；
2. 木马；
3. 隐藏管理员入口；
4. 隐藏账户；
5. 恶意脚本；
6. 未授权远程控制；
7. 未授权数据上传；
8. 隐藏网络请求；
9. 恶意追踪；
10. 未授权第三方数据收集；
11. 绕过用户权限的代码；
12. 绕过浏览器或操作系统安全机制的代码；
13. 与当前任务无关的隐藏功能；
14. 未经授权的外部通信；
15. 未经授权的数据导出或传输。

不得为了完成当前任务以外的目的，加入任何隐藏代码、隐藏请求、隐藏权限、隐藏账户或其他不可见功能。

不得加入与当前任务无关的外部脚本、远程资源、CDN、SDK、API或第三方服务。

任何新增第三方SDK、库、脚本、API、远程资源或其他依赖，在加入之前必须明确其：

1. 实际用途；
2. 来源；
3. 安全性；
4. 权限需求；
5. 数据处理方式；
6. 网络请求行为；
7. 对性能的影响；
8. 对隐私的影响；
9. 对跨平台兼容性的影响；
10. 许可证及使用限制。

如果第三方依赖并非完成当前任务所必需，则不得擅自加入。

十九、用户数据与隐私安全规范

任何涉及用户信息的数据处理功能，必须遵循合法、必要、透明和最小化原则，并与项目实际业务需求、用户协议及隐私政策保持一致。

未经项目负责人明确要求，不得擅自新增用户数据采集、存储、分析、追踪或向第三方传输。

不得擅自收集与当前功能无关的敏感或设备信息，包括但不限于：

1. GPS精确位置；
2. IMEI；
3. MAC地址；
4. 通讯录；
5. 短信；
6. 摄像头；
7. 麦克风；
8. 精确设备标识符；
9. 其他与当前功能无关的敏感数据。

如果某项用户数据确实属于明确的业务需求，例如注册IP、登录IP、设备类型、操作系统版本、注册时间、登录时间等，可以根据明确需求进行合理设计，但必须：

1. 明确数据用途；
2. 只收集完成业务所必要的数据；
3. 不擅自扩大采集范围；
4. 不擅自发送给第三方；
5. 考虑安全存储；
6. 考虑未来API、后端和数据库架构；
7. 确保实际数据处理方式与项目隐私政策保持一致。

任何AI、开发人员或第三方代码都不得自行判断“顺便收集一些用户数据比较有用”并擅自加入数据采集。

如果任务涉及新增用户数据、设备信息、网络信息、权限或第三方数据处理，而现有需求、用户协议或隐私政策没有明确覆盖，应先提出说明，由项目负责人确认后再实施。

二十、网络、API及异常处理

所有涉及网络、服务器、API、数据同步的功能必须考虑：

1. 网络延迟；
2. 网络波动；
3. 断网；
4. 请求超时；
5. 请求失败；
6. 服务器异常；
7. 数据格式错误；
8. 数据缺失；
9. API版本变化；
10. 重复请求；
11. 请求顺序问题；
12. 数据不同步。

必须设计合理的异常处理。

不能因为API请求失败而导致整个页面崩溃。

必须为用户提供合理、清晰、国际化的错误提示。

未来接入真实API时，前端应尽可能通过清晰的数据层或API服务层与页面业务进行分离。

二十一、数据结构与接口兼容

任何新增或修改数据结构时，必须考虑：

1. 当前功能兼容；
2. 现有数据兼容；
3. 未来API兼容；
4. 后端兼容；
5. 数据库兼容；
6. 管理后台兼容；
7. 国际化兼容；
8. 版本升级兼容。

禁止无理由修改已经被其他模块使用的数据字段。

如确实需要修改，应先检查所有调用方。

涉及字段名称、数据类型、默认值、状态值等重要变化时，应考虑未来数据迁移和版本兼容问题。

二十二、未来多人及服务器环境

所有功能必须考虑未来从单机测试环境发展到真实服务器环境。

不能只考虑当前一个用户运行的情况。

未来需要考虑：

1. 多用户同时在线；
2. 大量用户访问；
3. 并发请求；
4. 数据一致性；
5. 请求顺序；
6. 重复提交；
7. 服务端压力；
8. 数据库压力；
9. 缓存；
10. 网络异常；
11. 服务扩展。

当前没有后端时不要求提前实现这些服务器功能，但代码结构不得故意阻碍未来实现。

二十三、第三方依赖

新增任何第三方库、SDK、CDN、API、字体、图片资源或外部脚本之前，必须确认：

1. 是否真的需要；
2. 来源是否可靠；
3. 是否存在安全风险；
4. 是否影响性能；
5. 是否影响跨平台兼容；
6. 是否影响离线运行；
7. 是否产生额外网络请求；
8. 是否收集用户数据；
9. 是否存在授权或许可证问题。

未经明确要求，禁止擅自增加第三方依赖。

二十四、资源管理

图片、音频、字体、视频及其他资源必须合理管理。

不得：

1. 重复加载相同资源；
2. 使用无必要的大型资源；
3. 引入未知来源资源；
4. 产生资源路径错误；
5. 造成资源泄漏；
6. 因资源加载失败导致核心功能崩溃。

资源路径必须考虑不同部署环境。

不得只在当前本地路径下能够运行。

二十五、浏览器兼容

必须考虑主流现代浏览器兼容性。

包括但不限于：

1. Chrome；
2. Edge；
3. Safari；
4. Firefox；
5. 移动端主流浏览器。

使用浏览器新API时，应评估目标设备兼容情况。

对于存在兼容性差异的功能，应提供合理降级方案。

二十六、错误处理

任何可能出现异常的功能，都必须进行合理的错误处理。

禁止出现：

1. 未捕获异常导致页面崩溃；
2. Promise rejection未处理；
3. 请求失败后无限重试；
4. 用户点击后没有任何反馈；
5. 异常状态无法恢复；
6. 错误信息泄露敏感数据。

错误提示必须使用统一i18n。

二十七、日志与调试

开发过程中可以使用必要的调试日志。

正式版本不得保留：

1. 大量无意义console.log；
2. 敏感用户数据日志；
3. 密码日志；
4. Token日志；
5. 未授权调试入口；
6. 隐藏测试功能。

不得在前端日志中输出不必要的敏感数据。

二十八、账户、密码及认证相关原则

涉及注册、登录、密码、验证码、账户状态等功能时，必须考虑未来后端认证架构。

前端可以负责：

1. 表单输入；
2. 基础格式检查；
3. 用户体验；
4. 请求发送；
5. 状态显示。

但涉及真正的身份认证、密码验证、权限判断、验证码验证、账户状态判断等关键安全逻辑，未来必须由服务端进行最终验证。

不得把真正的认证安全逻辑仅放在前端。

不得在前端明文保存或输出用户密码。

二十九、功能边界

AI必须严格按照当前任务执行。

如果用户要求“只修改X”，则只能修改X相关内容。

不得自行：

1. 更换设计；
2. 重写其他功能；
3. 修改文案；
4. 修改语言；
5. 修改颜色；
6. 修改动画；
7. 修改布局；
8. 修改游戏规则；
9. 修改数据结构；
10. 修改其他模块。

除非这些修改是完成当前任务不可避免的必要修改。

如果确实需要影响其他模块，必须先说明原因。

三十、修改前检查

每一次修改代码之前，必须完成以下检查：

1. 阅读并理解本项目开发规范；
2. 分析当前项目结构；
3. 分析相关文件；
4. 分析相关模块；
5. 分析依赖关系；
6. 分析数据流；
7. 分析调用关系；
8. 分析现有功能；
9. 分析可能受到影响的其他功能；
10. 分析跨平台影响；
11. 分析性能影响；
12. 分析安全影响；
13. 分析国际化影响；
14. 分析未来API、后端、数据库及后台兼容性；
15. 确认修改范围。

确认后才能开始修改。

三十一、修改后检查

每次修改完成后，必须再次进行完整检查。

至少检查：

1. 当前功能是否正常；
2. 原有功能是否正常；
3. 其他页面是否正常；
4. UI是否正常；
5. 动画是否正常；
6. 交互是否正常；
7. 国际化是否正常；
8. 中文及其他语言是否正常；
9. 不同设备是否正常；
10. 不同系统是否正常；
11. 不同浏览器是否正常；
12. 性能是否下降；
13. 是否出现新的错误；
14. 是否产生新的安全风险；
15. 是否增加不必要的数据采集；
16. 是否增加未经授权的网络请求；
17. 数据结构是否仍然兼容；
18. 未来API是否仍然可以接入；
19. 未来后端是否仍然可以接入；
20. 未来数据库是否仍然可以接入；
21. 未来管理后台是否仍然可以接入；
22. 是否产生新的技术债务。

必须做到：

解决当前问题，同时不引入新的问题。

三十二、回归测试原则

任何修改都必须考虑回归测试。

即使当前任务只是修改一个按钮、一个颜色或一段文字，也必须确认修改没有影响其他功能。

特别是涉及公共组件、公共CSS、公共JavaScript、公共数据结构、公共配置、国际化资源时，必须检查所有使用该组件或资源的页面。

三十三、禁止自行扩大任务范围

AI在执行任务过程中，如果发现：

1. 其他代码可以优化；
2. UI可以优化；
3. 动画可以优化；
4. 性能可以优化；
5. 结构可以优化；
6. 其他功能存在问题；

不得直接修改。

必须先列出：

问题是什么；
为什么需要修改；
可能影响什么；
建议如何修改。

等待明确确认后才能执行。

三十四、版本与变更控制

重要功能和核心配置的修改应尽可能保持清晰的变更记录。

涉及重大数据结构、API、账户系统、游戏核心逻辑或安全逻辑的修改，不得在不了解影响范围的情况下直接覆盖。

如未来建立正式版本管理系统，应保留合理的版本号和变更记录。

三十五、禁止破坏现有稳定功能

项目中已经经过测试并稳定运行的功能，默认视为受保护功能。

任何修改都不得因为：

“代码更漂亮”
“结构更高级”
“更方便开发”
“顺便优化”

而擅自改变已经稳定运行的行为。

如果必须改变，必须明确说明改变原因及影响范围。

三十六、技术选型、安全漏洞与依赖安全

所有代码、框架、库、SDK、API、第三方依赖和技术方案，都必须优先选择成熟、稳定、持续维护且具有良好安全记录的方案。

禁止为了完成任务而主动使用：

1. 已停止维护且存在明显安全风险的技术；
2. 已知存在严重安全漏洞且没有合理修复方案的版本；
3. 已被官方明确废弃的技术；
4. 来源不明的第三方代码；
5. 来源不明的SDK；
6. 来源不明的脚本；
7. 来源不明的远程资源；
8. 明显存在恶意行为或安全风险的依赖。

使用第三方库、框架、SDK或其他依赖时，应优先使用当前仍在维护的稳定版本，并在技术上允许的情况下避免使用已知存在严重漏洞的版本。

如果某个依赖存在公开已知的严重安全漏洞，必须先检查是否存在安全更新或替代方案。

如果发现当前项目已经存在安全风险的依赖，不得为了完成其他无关任务而偷偷替换或升级。

应先向项目负责人明确说明：

1. 存在什么安全问题；
2. 影响哪个依赖；
3. 当前版本；
4. 风险等级；
5. 是否有官方修复版本；
6. 是否存在兼容性影响；
7. 推荐解决方案。

涉及安全敏感功能时，应优先使用经过长期验证的成熟安全方案，而不是为了追求所谓“高级代码”而使用复杂、实验性或没有必要的技术。

禁止认为“代码可以运行”就代表代码安全。

所有新增依赖必须考虑：

1. 安全性；
2. 维护状态；
3. 已知漏洞；
4. 来源；
5. 许可证；
6. 性能；
7. 浏览器兼容性；
8. 移动端兼容性；
9. 未来维护成本；
10. 是否真的有必要。

三十七、现代商业级视觉与审美规范

所有UI、游戏界面、动画、特效、按钮、弹窗、图标、字体、颜色、背景及整体视觉设计，必须符合2026年及未来现代商业级游戏产品的审美标准。

整体视觉目标：

1. 高级感；
2. 精致感；
3. 科技感；
4. 现代感；
5. 年轻化；
6. 商业级品质；
7. 视觉层次丰富；
8. 统一且完整的设计语言；
9. 良好的视觉反馈；
10. 高质量的细节表现。

禁止出现明显的：

1. 廉价感；
2. 土气设计；
3. 过时设计；
4. 粗糙设计；
5. 模板化设计；
6. 低质量素材；
7. 随意拼接的UI；
8. 不协调的颜色；
9. 过度使用渐变；
10. 过度使用发光；
11. 没有层次的纯色块；
12. 生硬的动画；
13. 低质量图标；
14. 不统一的字体；
15. 不统一的圆角；
16. 不统一的阴影；
17. 不合理的间距；
18. 视觉元素堆砌；
19. 为了“看起来炫”而加入大量无意义特效。

科技感不等于简单堆叠蓝色、霓虹灯、发光边框和粒子效果。

如果使用科技感视觉元素，必须考虑整体设计语言、光影关系、层次关系、比例、动效节奏和用户体验。

所有视觉优化必须做到：

高级但不过度、科技但不俗套、精致但不复杂、炫酷但不廉价。

三十八、技术高级感原则

“高级代码”不是代码越复杂越好。

必须优先选择：

1. 清晰；
2. 稳定；
3. 安全；
4. 高性能；
5. 可维护；
6. 可扩展；
7. 易调试；
8. 符合行业标准；

的实现方式。

禁止为了显示技术复杂度而加入没有必要的复杂架构。

禁止使用无法解释用途的复杂代码。

禁止使用过度设计。

真正的专业代码应当做到：

代码结构简单清晰，但架构合理；
功能实现稳定，但具有未来扩展能力；
技术方案成熟，但不过时；
视觉效果高级，但不依赖无意义的特效堆砌。

三十九、恶意脚本、攻击行为与代码安全

项目中的任何代码、脚本、接口、依赖或第三方组件，都不得包含或主动实现未经授权的攻击、入侵、控制、窃取或绕过安全机制的行为。

禁止加入：

1. 木马程序；
2. 后门程序；
3. 恶意脚本；
4. 病毒代码；
5. 勒索软件；
6. 未授权远程控制；
7. 未授权远程执行代码；
8. 未授权命令执行；
9. 未授权文件读取；
10. 未授权文件上传；
11. 未授权文件下载；
12. 未授权数据窃取；
13. 未授权数据上传；
14. 隐藏网络通信；
15. 隐藏API请求；
16. 隐藏管理员账户；
17. 隐藏管理员入口；
18. 绕过身份验证；
19. 绕过权限控制；
20. 绕过浏览器安全机制；
21. 绕过操作系统安全机制；
22. 绕过验证码或安全验证；
23. 未授权扫描；
24. 未授权攻击其他服务器；
25. DDoS或其他拒绝服务攻击代码；
26. 恶意自动化攻击；
27. 恶意注入代码；
28. 任何其他未经授权的攻击性功能。

任何网络请求都必须具有明确、可解释的业务用途。

禁止使用隐藏请求、隐藏域名、隐藏接口、动态拼接未知地址等方式进行无法解释的网络通信。

如果代码需要访问服务器、API、CDN或第三方服务，必须明确说明访问目的、访问地址、发送的数据类型以及数据用途。

四十、禁止屎山代码与不可维护代码

任何AI、开发人员都不得为了快速完成任务而制造难以维护的代码。

禁止出现：

1. 大量重复代码；
2. 大量复制粘贴代码；
3. 超长且职责混乱的函数；
4. 一个函数同时承担大量不同职责；
5. 无意义的嵌套；
6. 大量if/else堆叠；
7. 随意使用全局变量；
8. 随意修改全局状态；
9. 重复定义相同功能；
10. 无意义的临时变量；
11. 无意义的注释；
12. 被注释掉但长期保留的旧代码；
13. 无法解释用途的代码；
14. 无法追踪的数据来源；
15. 隐式依赖；
16. 循环依赖；
17. 重复事件监听；
18. 重复定时器；
19. 重复API请求；
20. 为解决一个小问题而增加大量复杂代码。

代码必须保持：

1. 结构清晰；
2. 职责明确；
3. 模块合理；
4. 命名清晰；
5. 逻辑容易理解；
6. 容易调试；
7. 容易测试；
8. 容易修改；
9. 容易扩展；
10. 容易交接给其他开发人员继续维护。

如果一个功能可以使用简单、稳定、清晰的方案完成，不得为了追求所谓“高级”而使用复杂方案。

四十一、代码可维护性原则

项目必须以长期维护为目标。

任何代码修改都必须考虑未来：

1. 修改功能；
2. 增加功能；
3. 删除功能；
4. 更换UI；
5. 增加语言；
6. 接入API；
7. 接入后端；
8. 接入数据库；
9. 接入管理后台；
10. 更换第三方依赖；
11. 修复安全问题；
12. 进行性能优化。

代码不得设计成只有当前开发人员或当前AI才能理解。

关键逻辑必须具有清晰的结构和合理的命名。

如果某个模块未来可能频繁修改，应尽可能与其他稳定模块进行合理隔离。

四十二、修改后的代码整洁要求

每次AI修改代码完成后，必须检查是否产生：

1. 重复代码；
2. 无用代码；
3. 无用变量；
4. 无用函数；
5. 无用依赖；
6. 无用事件监听；
7. 无用定时器；
8. 无用网络请求；
9. 临时调试代码；
10. 调试日志；
11. 注释掉的大量旧代码；
12. 无法解释的代码。

如果本次修改产生了上述问题，应在提交修改结果之前进行清理。

但清理代码时仍必须遵循最小改动原则，不得借机重构与当前任务无关的代码。

四十三、安全优先但不得过度设计

安全设计必须建立在真实业务需求基础上。

不能因为追求安全而加入大量没有必要的复杂代码。

也不能为了代码简单而忽略真实安全风险。

应优先采用：

成熟；
稳定；
经过验证；
容易维护；
符合行业标准；

的安全方案。

安全功能必须尽可能做到：

安全、清晰、稳定、可维护、可审计。

四十四、修改前检查

每一次修改代码之前，必须完成以下检查：

1. 阅读并理解本项目开发规范；
2. 分析当前项目结构；
3. 分析相关文件；
4. 分析相关模块；
5. 分析依赖关系；
6. 分析数据流；
7. 分析调用关系；
8. 分析现有功能；
9. 分析可能受到影响的其他功能；
10. 分析跨平台影响；
11. 分析性能影响；
12. 分析安全影响；
13. 分析国际化影响；
14. 分析未来API、后端、数据库及后台兼容性；
15. 确认修改范围。

确认后才能开始修改。

四十五、修改后检查

每次修改完成后，必须再次进行完整检查。

至少检查：

1. 当前功能是否正常；
2. 原有功能是否正常；
3. 其他页面是否正常；
4. UI是否正常；
5. 动画是否正常；
6. 交互是否正常；
7. 国际化是否正常；
8. 中文及其他语言是否正常；
9. 不同设备是否正常；
10. 不同系统是否正常；
11. 不同浏览器是否正常；
12. 性能是否下降；
13. 是否出现新的错误；
14. 是否产生新的安全风险；
15. 是否增加不必要的数据采集；
16. 是否增加未经授权的网络请求；
17. 数据结构是否仍然兼容；
18. 未来API是否仍然可以接入；
19. 未来后端是否仍然可以接入；
20. 未来数据库是否仍然可以接入；
21. 未来管理后台是否仍然可以接入；
22. 是否产生新的技术债务。

必须做到：

解决当前问题，同时不引入新的问题。

四十六、回归测试原则

任何修改都必须考虑回归测试。

即使当前任务只是修改一个按钮、一个颜色或一段文字，也必须确认修改没有影响其他功能。

特别是涉及公共组件、公共CSS、公共JavaScript、公共数据结构、公共配置、国际化资源时，必须检查所有使用该组件或资源的页面。

四十七、禁止自行扩大任务范围

AI在执行任务过程中，如果发现：

1. 其他代码可以优化；
2. UI可以优化；
3. 动画可以优化；
4. 性能可以优化；
5. 结构可以优化；
6. 其他功能存在问题；

不得直接修改。

必须先列出：

问题是什么；
为什么需要修改；
可能影响什么；
建议如何修改。

等待明确确认后才能执行。

四十八、版本与变更控制

重要功能和核心配置的修改应尽可能保持清晰的变更记录。

涉及重大数据结构、API、账户系统、游戏核心逻辑或安全逻辑的修改，不得在不了解影响范围的情况下直接覆盖。

如未来建立正式版本管理系统，应保留合理的版本号和变更记录。

四十九、禁止破坏现有稳定功能

项目中已经经过测试并稳定运行的功能，默认视为受保护功能。

任何修改都不得因为：

“代码更漂亮”
“结构更高级”
“更方便开发”
“顺便优化”

而擅自改变已经稳定运行的行为。

如果必须改变，必须明确说明改变原因及影响范围。

五十、专业开发原则

所有开发必须遵循以下核心原则：

1. 先理解，再修改；
2. 先分析，再执行；
3. 最小改动；
4. 不破坏已有功能；
5. 不擅自扩大范围；
6. 数据与业务逻辑合理分离；
7. 当前实现兼顾未来扩展；
8. 前端与未来API、后端、数据库、后台保持兼容；
9. 安全优先；
10. 隐私优先；
11. 性能优先；
12. 稳定性优先；
13. 可维护性优先；
14. 可扩展性优先；
15. 用户体验优先；
16. 技术方案优先选择成熟、稳定、持续维护的方案；
17. 禁止使用已知存在严重安全风险且没有合理修复方案的技术或依赖；
18. 禁止加入恶意代码、攻击代码、隐藏功能或未授权行为；
19. 禁止制造屎山代码；
20. 禁止为了所谓高级而进行无意义的复杂化；
21. UI和视觉必须符合2026年及未来的现代商业级审美；
22. 必须保持高级、精致、科技、现代的整体视觉品质；
23. 禁止廉价、土气、过时、粗糙、模板化设计。

五十一、最终执行流程

以后执行本项目任何任务时，无论任务属于：

功能添加、功能修改、功能删除、BUG修复、UI调整、动画优化、性能优化、安全优化、代码重构、代码升级、API接入、后端接入、数据库接入、后台接入或其他任何开发任务，都必须严格执行以下流程：

第一步：阅读并理解本规范。

第二步：完整分析当前项目。

第三步：确认当前任务的实际修改范围。

第四步：检查代码结构、模块依赖、数据流、调用关系及潜在影响。

第五步：检查跨平台、性能、安全、隐私、国际化及未来扩展问题。

第六步：检查未来API、后端、数据库和管理后台兼容性。

第七步：检查新增或修改的技术、库、SDK、脚本和依赖是否存在已知安全风险、维护风险或兼容性问题。

第八步：检查是否存在恶意代码、隐藏请求、隐藏功能、未授权数据采集或其他安全风险。

第九步：制定最小改动方案。

第十步：执行修改。

第十一步：检查修改结果。

第十二步：清理本次修改产生的无用代码、调试代码、重复代码和临时代码。

第十三步：进行功能回归检查。

第十四步：检查安全、性能、兼容性、数据结构及未来架构兼容性。

第十五步：确认没有产生新的问题后，才视为本次任务完成。

如果发现任务本身与现有架构存在冲突，或者无法在不破坏现有功能的情况下完成，必须先停止修改并说明问题，不得自行猜测或强行修改。

最终目标：

打造一个长期稳定、高质量、安全可靠、隐私合规、跨平台兼容、结构清晰、易于维护、易于扩展，并能够从当前前端阶段平稳发展到未来API、后端、数据库及管理后台架构的商业级游戏项目。

所有开发、修改、优化、重构和升级，必须严格按照本规范执行。

